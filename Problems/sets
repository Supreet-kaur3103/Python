#Due to the way they're stored, it's faster to check whether an item is part of a set using the in operator, rather than part of a list.
letters = {"a", "b", "c", "d"}
if "e" not in letters:
  print(1)
else: 
  print(2)
  
  
print("******************")
#adding and removind values
nums = {1, 2, 1, 3, 1, 4, 5, 6}
print("Original set:",nums)
nums.add(-7)
nums.remove(3)
print("Adding -7 and removing 3:",nums)


print("***********************")
#The union operator | combines two sets to form a new one containing items in either. 
#The intersection operator & gets items only in both. 
#The difference operator - gets items in the first set but not in the second. 
#The symmetric difference operator ^ gets items in either set, but not both. 
first = {1, 2, 3, 4, 5, 6}
second = {4, 5, 6, 7, 8, 9}

print("Union:", first | second) #union
print("Intersection:" ,first & second) #intersection
print("Diff -> First - Second:",first - second) #diff
print("Diff -> Second - First:",second - first) #diff
print("Symmetric Difference:",first ^ second) #symmetric diff


